<?php

namespace cls\data\space;

/**
 * List of official spaces, that populate the main feed.
 * This list is hard coded, so we focus the attention of the
 * society on the most important spaces for collective survival.
 */
final class OfficialSpaces {
  # We don't want this class to be instantiated.
  private function __construct() {}
  
  const OFFICIAL_SPACES = [
    ## map the spaces on the id ...
  ];
}