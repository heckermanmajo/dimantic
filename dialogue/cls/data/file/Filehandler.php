<?php

namespace cls\data\file;

/**
 * All files are also put into the database.
 *
 * todo: dont allows pdf and convert odt and so on to
 * todo: to markdown via droplet and then store as string in the
 *       database.
 * todo: allow image uploads and store as blobs in database, not as files.
 */
class FileHandler {

}